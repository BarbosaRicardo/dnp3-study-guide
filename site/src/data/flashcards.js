export const FLASHCARDS = {
  intro: [
    { q: 'What does DNP3 stand for?', a: 'Distributed Network Protocol 3. The "3" is because versions 1 and 2 were internal drafts — never shipped publicly. Classic engineers.' },
    { q: 'Who developed DNP3 and when?', a: 'Westronic Inc., circa 1990. They based it on IEC 60870-5 (European standard) and adapted it for North American electric utilities.' },
    { q: 'What were the two original physical media DNP3 targeted?', a: 'RS-232 (point-to-point serial) and RS-485 (multidrop serial). TCP/IP support came later as an extension.' },
    { q: 'What is the default TCP port for DNP3?', a: 'Port 20000 — for both TCP and UDP. Port 502 is Modbus. Port 102 is IEC 61850. Know them all so you can write firewall rules without Googling.' },
    { q: 'What are the two roles in a DNP3 network?', a: 'Master (polls, commands, initiates) and Outstation (responds, reports, executes). DNP3 uses these terms — not master/slave or client/server.' },
    { q: 'How many unique device addresses does DNP3 support?', a: '65,519 (0x0000 to 0xFFF9). Compare to Modbus: 247. DNP3 was built for large utility networks spanning entire states.' },
    { q: 'What is the DNP3 broadcast address?', a: '0xFFFF. All outstations process frames addressed to it — but NEVER respond. Responding would cause RS-485 bus collisions.' },
    { q: 'What are DNP3 data quality flags?', a: 'Per-point status bits: Online, Restart, Comm-Lost, Remote-Forced, Local-Forced, Over-Range. They tell you whether a value is trustworthy — Modbus gives you nothing like this.' },
    { q: 'What is a DNP3 Device Profile Document?', a: 'A vendor-supplied spec sheet listing exactly which function codes, object groups, and optional features a device supports. Request it before integrating any new outstation.' },
    { q: 'How wide are DNP3 timestamps, and how far do they extend?', a: '48-bit millisecond UTC values. Covers dates until the year 10,889. Whoever designed DNP3 was not going to repeat Y2K.' },
    { q: 'What two hex bytes start every DNP3 frame?', a: '0x05 0x64 — always, without exception. The protocol\'s secret handshake. Scan for these to find DNP3 frames in any byte stream.' },
    { q: 'What industry first adopted DNP3?', a: 'Electric utilities — substations, transmission lines, distribution automation. It later spread to water/wastewater and oil & gas.' },
  ],

  layers: [
    { q: 'Name the three DNP3 protocol layers from bottom to top.', a: 'Data Link Layer → Transport Layer → Application Layer. No network layer — DNP3 was designed for serial, not routed networks.' },
    { q: 'What is the purpose of the DNP3 Transport Layer?', a: 'Fragmentation and reassembly. Large Application messages that exceed one Data Link frame get split into transport segments, then reassembled at the other end.' },
    { q: 'What does the Transport Layer header contain?', a: 'One byte: FIR (first segment), FIN (last segment), and a 6-bit sequence number. That\'s it — simple by design.' },
    { q: 'What does the Data Link Layer add for reliability?', a: 'CRC-16/DNP error checking on every 16-byte data block. Block-level checking catches corruption anywhere in the frame, not just end-to-end.' },
    { q: 'What is the maximum DNP3 Data Link frame size?', a: '292 bytes: 10-byte header + up to 282 bytes of user data (in 16-byte blocks, each with a 2-byte CRC). This limit drives the need for Transport Layer segmentation.' },
    { q: 'What is the DNP3 Application Layer responsible for?', a: 'Function codes (READ, WRITE, SELECT, OPERATE), data object groups and variations, IIN bits, and all request/response semantics. Where DNP3 actually "talks SCADA."' },
    { q: 'What does FIR=1, FIN=1 on the same Application fragment mean?', a: 'The entire Application message fits in one fragment — no fragmentation needed. Most small responses look like this.' },
    { q: 'What are the three PDU acronyms in DNP3, one per layer?', a: 'APDU (Application), TPDU (Transport), LPDU (Link). Application produces APDUs → Transport produces TPDUs → Data Link produces LPDUs.' },
    { q: 'What does the DIR bit in the Data Link Control byte indicate?', a: 'Direction. DIR=1 = frame from master (primary). DIR=0 = frame from outstation (secondary). Critical for analyzing traffic on a shared RS-485 bus.' },
    { q: 'When DNP3 runs over TCP, is the Data Link framing (0x05 0x64, CRCs) preserved?', a: 'Yes — the complete DNP3 Data Link frame, start bytes and all, rides inside TCP as payload. TCP adds its own checksum, but DNP3 keeps its CRCs too.' },
  ],

  datalink: [
    { q: 'What are the two sync bytes that begin every DNP3 Data Link frame?', a: '0x05 0x64 — the frame boundary marker. Every parser starts by scanning for these two consecutive bytes.' },
    { q: 'How long is the DNP3 Data Link header?', a: '10 bytes: 2 sync + 1 length + 1 control + 2 dest address + 2 src address + 2 CRC. The CRC covers the first 8 bytes of the header.' },
    { q: 'What does the FCB (Frame Count Bit) do?', a: 'Alternates (0→1→0→1) with each new primary frame. If the receiver gets two consecutive frames with the same FCB, it knows the second is a retransmit and discards it.' },
    { q: 'What is the maximum user data payload per DNP3 Data Link data block?', a: '16 bytes per block, followed by 2 CRC bytes = 18 bytes per chunk. This granularity means a corrupted byte anywhere fails that specific block\'s check.' },
    { q: 'What does Primary FC=0 (RESET_LINK_STATES) do?', a: 'Resets the link layer state machine on both sides and synchronizes the FCB. It\'s the first thing a master sends to establish communication — always start here.' },
    { q: 'What is the difference between CONFIRMED and UNCONFIRMED user data?', a: 'CONFIRMED (FC=3) requires an ACK from the receiver; retransmitted if no ACK. UNCONFIRMED (FC=4) is fire-and-forget — no ACK expected. Broadcasts always use FC=4.' },
    { q: 'What does Secondary FC=0 (ACK) mean?', a: '"I received your frame successfully and I\'m ready for more." The master expects ACK for every confirmed primary frame. No ACK within timeout = retransmit.' },
    { q: 'What does Secondary FC=1 (NACK) mean?', a: '"I got something but couldn\'t process it — buffer full or CRC failed." The master retransmits the same frame with the same FCB (FCB only toggles on new frames).' },
    { q: 'What CRC polynomial does DNP3 use?', a: 'CRC-16/DNP, polynomial 0x3D65. It is NOT CRC-16/Modbus (0x8005). Using the wrong CRC implementation means every frame fails — silently.' },
    { q: 'What does FCV=0 mean in the Data Link Control byte?', a: '"Ignore the FCB field for this frame type." Certain frames like RESET_LINK_STATES don\'t use the FCB toggle, so FCV=0 signals the receiver not to check it.' },
    { q: 'What is REQUEST_LINK_STATUS (Primary FC=9) used for?', a: 'Ask the outstation: "Are you there and initialized?" Outstation responds with LINK_STATUS. Used as a health check without sending actual data.' },
  ],

  appLayer: [
    { q: 'What is an APDU?', a: 'Application Protocol Data Unit — the complete Application Layer message. Contains the function code, application control byte, optional IIN bits, and all data objects.' },
    { q: 'What four flags are in the DNP3 Application Control (AC) byte?', a: 'FIR (first fragment), FIN (final fragment), CON (confirmation requested), UNS (unsolicited response). Plus a 4-bit sequence number.' },
    { q: 'Which IIN bit signals a device restart?', a: 'IIN1.6 = Device Restart. Must trigger: time sync → Class 0 poll → re-enable unsolicited → clear IIN1.6. Skip any step and you get stale data or missed events.' },
    { q: 'Which IIN bit means "I need a time synchronization"?', a: 'IIN1.7 = Need Time. Events logged while this is set have inaccurate timestamps. Respond immediately with a G50V2 WRITE.' },
    { q: 'Which IIN bit signals Class 1 events are waiting?', a: 'IIN2.0 = Class 1 Events Available. IIN2.1 = Class 2. IIN2.2 = Class 3. A good master polls for Class 1 immediately when it sees IIN2.0 — not at the next scheduled interval.' },
    { q: 'What is the Application Layer function code for READ?', a: 'FC=1. This is the workhorse of DNP3 polling. "READ G60V1 Qual 0x06" = "give me all Class 0 static data."' },
    { q: 'What does CON=1 in the Application Control byte request?', a: 'The sender wants the receiver to send an Application Layer CONFIRM (FC=0) back. Used for unsolicited responses — if the master doesn\'t confirm, the outstation retransmits.' },
    { q: 'What does Qualifier Code 0x06 mean in a DNP3 READ request?', a: '"All points" — no start/stop index, no count. "Give me everything for this Group/Variation." Standard qualifier for Class 0 polls.' },
    { q: 'How wide are DNP3 Application Layer sequence numbers?', a: '4 bits — values 0 through 15, then wraps. Both sides track this to match responses to requests and detect duplicates.' },
    { q: 'What is "Class 0" data in DNP3?', a: 'The static snapshot — current value of all configured points right now. Not event-driven. Polled with READ G60V1. Class 1/2/3 are event buffers.' },
    { q: 'What function code sends time synchronization to an outstation?', a: 'FC=2 (WRITE) with object G50V2 (Time and Date). The master writes the current UTC time. No separate time protocol — just a plain WRITE.' },
  ],

  objects: [
    { q: 'What does "Group" identify in DNP3 data objects?', a: 'The data TYPE: Binary Input (G1), Binary Output Status (G10), Binary Counter (G20), Analog Input (G30), Analog Output Status (G40). Group = WHAT.' },
    { q: 'What does "Variation" specify in DNP3 data objects?', a: 'The FORMAT within a Group. G1V1 = Binary Input without flags. G1V2 = with flags. G30V1 = 32-bit analog. G30V5 = float. Same data type, different detail level.' },
    { q: 'What is Variation 0 (e.g., G1V0) used for?', a: '"Let the outstation pick the best variation for this Group." Not all devices support V0 — check the Device Profile Document first.' },
    { q: 'What is Group 2 in DNP3?', a: 'Binary Input Change Events. G1 is current state (static). G2 is what changed and when. G2V2 includes an absolute timestamp — the moment the change was detected at the outstation.' },
    { q: 'What is a DNP3 CROB?', a: 'Control Relay Output Block — Group 12. The command object for operating binary outputs. Contains: Control Code (LATCH_ON/OFF, PULSE_ON/OFF), Count, On-Time, Off-Time, and Status.' },
    { q: 'What does CROB Control Code 0x01 (LATCH_ON) do?', a: 'Energizes the output and holds it until LATCH_OFF (0x02) is sent. PULSE_ON (0x03) energizes for On-Time milliseconds then de-energizes automatically.' },
    { q: 'What is Group 60 used for?', a: 'Class Assignment polling. G60V1 = Class 0 (all static). G60V2 = Class 1 events. G60V3 = Class 2. G60V4 = Class 3. The standard way masters request data by class.' },
    { q: 'What is the difference between Group 10 and Group 12?', a: 'G10 = Binary Output Status (READ the current state of a digital output). G12 = CROB (COMMAND a digital output). Read with G10; control with G12. Never mix them up on a live substation.' },
    { q: 'What does the "Online" quality flag being cleared (0) mean?', a: 'The point is not online — the value is stale, invalid, or the input card is faulted. Do not rely on it for operations decisions.' },
    { q: 'What is the "Comm-Lost" quality flag?', a: 'The outstation lost communication with a downstream sub-device that provides this data point. The value shown is whatever it was last measured at — could be hours old.' },
    { q: 'What is Group 32?', a: 'Analog Input Change Events. Triggered when an analog value changes beyond its configured deadband. Without deadbands, a noisy sensor generates thousands of events per hour.' },
    { q: 'What is a DNP3 Frozen Counter (Group 21)?', a: 'A counter value captured at a specific instant (usually when the master sends a FREEZE command). Held until the next freeze. Used for interval energy billing reads.' },
  ],

  fc: [
    { q: 'What is the DNP3 Application Layer function code for CONFIRM?', a: 'FC=0. Sent by the master to acknowledge an outstation\'s unsolicited response. Without CONFIRM, the outstation retransmits. Endlessly.' },
    { q: 'What is FC=1?', a: 'READ. The polling workhorse. "READ G60V1 Qual 0x06" = Class 0 poll. Almost all data acquisition uses FC=1.' },
    { q: 'What is FC=2?', a: 'WRITE. Used for time sync (G50V2), analog deadbands (G34), and IIN bit clears. WRITE is not a control command — it bypasses the SBO safety interlock.' },
    { q: 'What are FC=3 and FC=4?', a: 'FC=3 = SELECT (step 1 of SBO). FC=4 = OPERATE (step 2). SELECT first, get confirmation, then OPERATE within the timeout window (typically 10 seconds).' },
    { q: 'What is FC=5?', a: 'DIRECT_OPERATE. Single-step command — no SELECT required. Faster but no safety interlock. Use SBO for breakers and switches. Use DIRECT_OPERATE only where a mis-fire is acceptable.' },
    { q: 'What is FC=13?', a: 'COLD_RESTART. Full device restart — clears RAM, reinitializes everything. After restart, IIN1.6 will be set. Use with extreme caution on live substations.' },
    { q: 'What is FC=14?', a: 'WARM_RESTART. Soft reboot — may preserve event buffer and configured state. Less disruptive than COLD_RESTART. Behavior is implementation-specific; check vendor docs.' },
    { q: 'What is FC=20?', a: 'ENABLE_UNSOLICITED. Tells the outstation to start sending event data without being polled. Specify which classes to enable (G60V2/V3/V4). Disabled by default on most outstations.' },
    { q: 'What is FC=21?', a: 'DISABLE_UNSOLICITED. Send this before taking a master offline for maintenance. Events keep buffering — they\'ll be delivered when the master comes back and re-enables unsolicited.' },
    { q: 'What is FC=7?', a: 'IMMED_FREEZE. Tells the outstation to freeze its counter values right now. Variants: FC=8 (no ACK), FC=9 (freeze at time), FC=11 (freeze and clear). Used for interval energy metering.' },
    { q: 'What happens when an outstation gets an FC it doesn\'t support?', a: 'It responds with IIN2.6 (Function Code Not Supported) set. The message was received — the device just can\'t do it. Check the Device Profile Document before sending uncommon FCs.' },
    { q: 'What is the SBO timeout and what happens when it expires?', a: 'Typically 10 seconds. If OPERATE doesn\'t arrive before the window closes, the outstation discards the SELECT and rejects the OPERATE. The master must SELECT again to retry.' },
  ],

  unsol: [
    { q: 'What is a DNP3 unsolicited response?', a: 'Data sent by the outstation to the master WITHOUT the master polling first. Event-driven delivery — the outstation reports changes as they happen. Like the newspaper thrown at your door instead of calling to ask if there\'s news.' },
    { q: 'Are unsolicited responses enabled by default?', a: 'No — disabled by default. The master must explicitly enable them with FC=20 (ENABLE_UNSOLICITED). This prevents outstations from flooding a master that hasn\'t connected yet.' },
    { q: 'How does the master acknowledge an unsolicited response?', a: 'FC=0 (CONFIRM) with UNS=1 and the matching sequence number. If the master doesn\'t confirm within the timeout, the outstation retransmits up to the configured retry count.' },
    { q: 'What is a Null Unsolicited Response?', a: 'An unsolicited message with NO event data — just IIN bits. Sent by the outstation after a restart or when unsolicited is first enabled. The master must confirm it before real events start flowing.' },
    { q: 'What is the UNS bit in the Application Control byte?', a: 'UNS=1 marks the message as unsolicited. The master uses this to distinguish unsolicited reports (no matching pending request) from normal solicited responses.' },
    { q: 'What triggers an unsolicited response from the outstation?', a: 'Either the event count threshold OR the hold time expires — whichever comes first. OR logic: many events = send quickly; few events but time passed = send anyway.' },
    { q: 'What is the correct order before enabling unsolicited (FC=20)?', a: 'Poll Class 0 first to establish a baseline. Then enable unsolicited. Without the baseline, unsolicited events represent changes from an unknown starting state.' },
    { q: 'What happens to the event buffer when unsolicited retries are exhausted?', a: 'Events remain in the buffer — they are NOT discarded. The outstation waits. When the master reconnects and polls (or confirms the next unsolicited), buffered events are delivered.' },
    { q: 'Why can\'t outstations respond to broadcast messages with unsolicited data?', a: 'All devices on a multidrop RS-485 bus would transmit simultaneously — bus collision. Rule: no responses to broadcasts, ever.' },
    { q: 'Which IIN bit indicates event buffer overflow?', a: 'IIN2.3 = Event Buffer Overflow. Events were lost because the buffer filled up before the master polled. Your historical record has gaps. Poll faster or increase buffer size.' },
    { q: 'What does DISABLE_UNSOLICITED (FC=21) do to the event buffer?', a: 'Nothing — events keep accumulating in the buffer. DISABLE_UNSOLICITED only stops the outstation from proactively sending them. Events are still returned when the master polls.' },
  ],

  security: [
    { q: 'What threat does DNP3 Secure Authentication protect against?', a: 'Unauthorized commands and spoofed data from malicious actors. Without SA, anyone on the network speaking DNP3 can send commands to outstations. SA adds cryptographic proof of identity.' },
    { q: 'What are the two versions of DNP3 Secure Authentication?', a: 'SA v2 (original) and SA v5 (current, aligned with IEC 62351-5). They are NOT backward compatible. Mixed environments will fail to authenticate — silently.' },
    { q: 'What is a "Challenge" in DNP3 SA?', a: 'A random nonce sent by the outstation to the master when it receives a critical message. The master must respond with a MAC computed from the nonce + shared key. Random = replay-proof.' },
    { q: 'What is a MAC in DNP3 SA?', a: 'Message Authentication Code — a cryptographic hash of (challenge data + Update Key). The key never travels over the wire. If MAC matches, the sender is authenticated.' },
    { q: 'What is an Update Key?', a: 'A symmetric pre-shared secret provisioned into both master and outstation at commissioning. Used to derive Session Keys. Must be rotated periodically per NERC CIP requirements.' },
    { q: 'Which DNP3 messages trigger an SA challenge?', a: '"Critical" function codes: OPERATE, DIRECT_OPERATE, COLD_RESTART, and others that actually DO something dangerous. READ requests are NOT typically challenged — that would make the network unusable.' },
    { q: 'What is SA Aggressive Mode?', a: 'The master pre-computes a MAC and includes it in the original request, eliminating the challenge round-trip. Falls back to normal challenge/response if the prediction fails.' },
    { q: 'What are Session Keys?', a: 'Short-lived keys derived from Update Keys via a key exchange. They expire after a configured number of uses or time. Limits exposure if a key is compromised.' },
    { q: 'Which DNP3 Group contains all SA objects?', a: 'Group 120. G120V1 = Challenge, G120V2 = Reply (MAC), G120V3 = Aggressive Mode, G120V5 = Session Key Change, G120V7 = Error.' },
    { q: 'What hash algorithm does SA v5 use?', a: 'HMAC-SHA-1 or HMAC-SHA-256. SHA-256 is preferred for new deployments. AES-128 is used for key wrapping (encrypting key change messages), not for MACs.' },
    { q: 'What does SA v5\'s "User Number" enable?', a: 'Per-user authorization. Each user has their own Update Key and authorization level. The outstation can grant User 1 full control and User 2 read-only — without a separate directory service.' },
  ],

  troubleshoot: [
    { q: 'An outstation isn\'t responding at all. What\'s the first thing to check?', a: 'Data Link Layer. Send RESET_LINK_STATES and look for a response. If nothing: physical wiring, baud rate, or address. Don\'t debug Application Layer until the link layer responds.' },
    { q: 'The master sends frames and gets nothing back (RS-485 multidrop). Most likely cause?', a: 'Address mismatch — the outstation hears every frame but ignores ones addressed to a different device number. Verify master config against outstation config.' },
    { q: 'Wireshark shows CRC errors on random bytes throughout frames. What\'s wrong?', a: 'Baud rate mismatch. The receiver samples bits at wrong times, corrupting bytes randomly. Check baud, data bits, parity, stop bits on BOTH ends. 9600-N-8-1 vs 9600-E-8-1 is a classic failure.' },
    { q: 'IIN1.6 (Device Restart) appears in a response. What must the master do?', a: '1) Send time sync (G50V2 WRITE). 2) Poll Class 0. 3) Re-enable unsolicited (FC=20). 4) Write to clear IIN1.6. In that order. Skipping steps = stale data or wrong timestamps.' },
    { q: 'IIN2.3 (Event Buffer Overflow) is set. What does this mean?', a: 'Events were lost because the buffer filled before the master polled them. Your historian has gaps. Fix: increase poll frequency, grow the buffer size, or enable unsolicited for faster delivery.' },
    { q: 'An OPERATE command returns Status Code 0x04. What happened?', a: 'NOT_SUPPORTED — the outstation doesn\'t support operating this point with this function code. Common cause: DIRECT_OPERATE on an SBO-only point, or wrong Group for a control type.' },
    { q: 'Pings succeed to an RTU but the master shows COMM_LOST for all points. Why?', a: 'Most likely: master address filter. The RTU only responds to a specific master address. If source address doesn\'t match, the RTU silently ignores everything. ICMP (ping) has nothing to do with DNP3.' },
    { q: 'TCP connections to an outstation drop every 30 seconds and reconnect. Cause?', a: 'NAT/firewall idle TCP timeout. The firewall drops "idle" connections before the next DNP3 poll fires. Fix: reduce poll interval, enable TCP keepalives, or adjust firewall idle timeout policy.' },
    { q: 'All analog values show -9999 and have the Comm-Lost quality flag. Why?', a: 'The RTU lost communication with the sub-device measuring these points (Modbus, GOOSE, etc.). The -9999 is the configured "not available" default value. Check sub-device wiring and communications.' },
    { q: 'Events are timestamped exactly 5 hours off. What\'s the most likely cause?', a: 'A time zone offset is being applied to UTC timestamps. DNP3 timestamps are always UTC. Check if SCADA software is converting to local time, or if a new firmware version changed the time zone setting.' },
    { q: 'An outstation sends unsolicited responses but the master never confirms. What happens?', a: 'Outstation retransmits up to the configured retry count, then stops. Events remain in the buffer. When the master comes back online, buffered events are delivered on the next poll or unsolicited attempt.' },
    { q: 'SELECT succeeds, then OPERATE returns NO_SELECT. Why?', a: 'The CROB object in OPERATE didn\'t exactly match the SELECT. Every byte must be identical: point index, control code, count, on-time, off-time. One mismatch = rejection. Re-SELECT to retry.' },
  ],

  lab: [
    { q: 'What should be the first test in any DNP3 lab setup after configuring addresses?', a: 'RESET_LINK_STATES. If you get a Data Link ACK, physical layer and addressing are correct. Then try Class 0. Then events. Then controls. Build up layer-by-layer.' },
    { q: 'What is the byte order in a DNP3 Data Link header?', a: '[0x05][0x64][LENGTH][CONTROL][DEST_LOW][DEST_HIGH][SRC_LOW][SRC_HIGH][CRC_LOW][CRC_HIGH]. 10 bytes total before the first data block.' },
    { q: 'What Wireshark display filter shows only DNP3 traffic?', a: '"dnp3" — lowercase, no quotes in the filter bar. Wireshark has had a DNP3 dissector since v1.4. It decodes every Group, Variation, function code, and IIN bit.' },
    { q: 'How do you request only Binary Input point 5 from an outstation?', a: 'READ G1V2, Qualifier 0x00 (start/stop range), start=5, stop=5. G1V2 includes quality flags. Qualifier 0x06 (all) would also work but returns every Binary Input.' },
    { q: 'You send a Class 0 poll. What object header do you build?', a: 'READ G60V1 Qualifier 0x06. G60V1 = Class 0 static data. Qualifier 0x06 = All. That single object header requests every static data point the outstation has configured.' },
    { q: 'What is the master startup sequence before enabling unsolicited?', a: '1) Establish connection. 2) RESET_LINK_STATES. 3) Poll Class 0. 4) Send time sync. 5) ENABLE_UNSOLICITED (FC=20). Order matters — baseline before events.' },
    { q: 'After ENABLE_UNSOLICITED, what must the outstation send first?', a: 'A Null Unsolicited Response — no event data, just IIN bits. After the master confirms it, real event-bearing unsolicited messages begin. The Null Unsolicited is mandatory, not optional.' },
    { q: 'In a Python DNP3 sim, what causes OPERATE to fail after a fast SELECT?', a: 'SBO timeout. If script logic (logging, blocking calls) delays the OPERATE past the 10-second window, the outstation rejects it. Keep SELECT-to-OPERATE code fast — no blocking calls in between.' },
    { q: 'What Qualifier Code specifies an 8-bit count of objects in a READ request?', a: 'Qualifier 0x17 = 8-bit count. The next byte is how many objects to return. Used for "give me up to N events" requests. 0x28 = 16-bit count.' },
    { q: 'What is the CRC-16/DNP polynomial in hex?', a: '0x3D65 (or 0xA6BC in reflected form). NOT 0x8005 (Modbus). Implement the wrong one and every frame fails with no useful error message.' },
    { q: 'A DNP3 Length byte reads 0x05. What does this mean?', a: 'Minimum-length frame — just the header, no user data. 1 control + 2 dest + 2 src = 5 bytes. This is the size of ACK, NACK, and link-layer-only responses.' },
    { q: 'When building a DNP3 outstation sim, what must it do after receiving ENABLE_UNSOLICITED?', a: '1) Send a normal Application response confirming FC=20 was received. 2) Send a Null Unsolicited Response (CON=1, no events). 3) Wait for master CONFIRM. 4) Begin unsolicited event delivery.' },
  ],
}
